# -----------------------------------------------------------------------------
# APP CONFIGURATION
# -----------------------------------------------------------------------------

# The port the application server (Vite/Serve) will listen on.
# Default: 8080
PORT=8080

# The host address to bind to. Use 0.0.0.0 for Docker/Container environments.
# Default: localhost
HOST=0.0.0.0

# -----------------------------------------------------------------------------
# SUPABASE CONFIGURATION (Required)
# -----------------------------------------------------------------------------
# Retrieve these from your Supabase Project Dashboard -> Project Settings -> API

# The URL of your Supabase project instance.
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# The 'anon' / public key for your Supabase project.
# Safe to expose in the client bundle.
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# -----------------------------------------------------------------------------
# SPOTIFY CONFIGURATION (Optional - for Auto-fill features)
# -----------------------------------------------------------------------------
# Retrieve these from Spotify Developer Dashboard (https://developer.spotify.com/)

# Spotify App Client ID
VITE_SPOTIFY_CLIENT_ID=your_spotify_client_id

# Spotify App Client Secret
VITE_SPOTIFY_CLIENT_SECRET=your_spotify_client_secret

# -----------------------------------------------------------------------------
# DEPLOYMENT NOTES
# -----------------------------------------------------------------------------
# If deploying via Docker, these variables are passed during the build or run phase.
# If deploying via Vercel/Netlify, add these to the platform's Environment Variables settings.