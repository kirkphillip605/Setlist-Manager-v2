import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.setlistmanagerv2',
  appName: 'Setlist-Manager-v2',
  webDir: 'dist'
};

export default config;
